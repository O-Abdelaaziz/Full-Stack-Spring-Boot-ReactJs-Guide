import React from 'react';
import BooksList from "./BooksList";

const Books = (props) => {

    return (
        <React.Fragment>
            <BooksList/>
        </React.Fragment>
    );
};

export default Books;